# TODO

- On the command-line, list the names of the test suites and fixtures that should be run:
  
  ``./my_test_app --run=Suite1,Suite2/Fixture1``

- Add support for test filters, so that you can run only a subset of the tests in a suite or fixture.

  ``./my_test_app --run=Suite1/Fixture1/Test1,Test2``

